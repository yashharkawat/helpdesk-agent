# Required for the agent. Only ":free" OpenRouter models are ever called; the client throws on any other model id.
OPENROUTER_API_KEY=
# Global cap on agent turns per warm instance per day.
HELPDESK_DAILY_TURN_BUDGET=150
